# 2026-08-11 工作日志

## 秋招工作台 - 接入真实求职数据采集

### 完成内容
- 新增 Python 多源采集器 `collector/` 模块，支持 3 个渠道：
  - 牛客网 (Playwright 浏览器自动化，应对 CSR 渲染)
  - 应届生求职网 (requests + BeautifulSoup，SSR 页面 GBK 编码)
  - Boss 直聘 (Playwright 浏览器自动化)
- 新增 GitHub Actions 工作流 `.github/workflows/daily-collect.yml`，每天 08:00 (UTC+8) 自动运行
- 前端 `workspace.js` 新增 `fetchRemoteJobs()` 函数，从 GitHub Raw URL 拉取 `jobs.json`
- 前端新增「从采集源刷新」按钮，含采集状态指示灯
- 新增 `jobs-sample.json` (25 个预整理 AI 产品/设计岗位) 和初始 `jobs.json`
- 采集器与前端使用一致的 4 字段去重逻辑 (公司+岗位+城市+批次)
- 更新 `README.md` 文档
- 已 push 到 GitHub (`b8dba13`)

### 文件变更
14 files changed, 1520 insertions(+), 9 deletions(-)

---

## 续：稳定性修复 + 真实数据验证（下午）

### 问题
- 此前 4 个本地 commit 实际未 push（git 显示 ahead of origin 4 commits）。
- Workflow run #31478386643 退出码 2：`COUNT` 提取 heredoc 失败 + 历史数据合并逻辑写反（导致「新增 -5」丢数据）。
- 首次真实运行 (#31485196417 成功) 暴露两个质量问题：
  1. 岗位匹配过宽——"热设计工程师"因含"设计"被误判为目标岗（应排除热/硬件/结构/光学/机械设计、算法、UI开发 等）。
  2. 仅牛客网产出 2 条真实岗位；应届生求职网、Boss 直聘均为 0。

### 修复
- `collector/config.py`：将 `ROLE_KEYWORDS` 拆分为 `ROLE_POSITIVE` + `ROLE_NEGATIVE`，`matches_role()` 与 `priority_score()`（新增 `role_type()`）均先排除负向词。本地单测 9/9 通过。
- `collector/sources/yingjiesheng.py`：解析由脆弱的 `<li>` 选择器改为扫描全部 `<a>` 链接，更稳。
- `collector/sources/playwright_collector.py`：Boss 去掉 `experience` 过滤、增加登录/验证拦截检测与更长等待。
- `jobs.json` 种子改为全北京、设计/产品混合（去掉深圳/杭州样本）。
- `.github/workflows/daily-collect.yml`：COUNT 用 heredoc 写入 `$GITHUB_OUTPUT`；0 条结果保护。

### 验证
- 本地回归测试 merge 逻辑：历史 6 + 新 2 = 8，排序正确。
- 全部 push 至 GitHub（含 rebase 解决 jobs.json 冲突，保留本地北京样本）。
- 已触发第二次 workflow 运行，等待结果确认相关性修复生效。

### 部署
- GitHub Pages 已启用：`https://r-zhang24-rivi.github.io/zhangrui-resume-editor/`（source: main /）。
- 每日 08:00 (UTC+8) 自动采集；也可 `gh`/API 手动 `workflow_dispatch` 触发。

---

## 续：第三次运行问题诊断 + 应届生改为 Playwright（傍晚）

### 第二次运行 (#31485915477) 结果
- 成功，产出 jobs.json 共 7 条（6 种子 + 1 牛客网真实岗「姚记·游戏UI设计师-27届秋招·北京」）。
- 相关性修复生效：无"热设计工程师"类误判。
- 但覆盖率仍薄：牛客网 1 条；应届生 0；Boss 0（报 `Page.query_selector_all: Execution context was destroyed` 跳转错误）。

### 诊断
- 应届生 `commend-fulltime` 列表页已是 **JS 渲染空壳**（静态 HTML 0 个 href、0 个 job 关键字），`requests` 抓不到内容 → 必须改用 Playwright 渲染。
- Boss 在 CI 出口 IP 易触发反爬/登录跳转，`query_selector_all` 在等待期间页面跳转导致报错。

### 修复（commit aa5e796，已 push）
- `collector/sources/yingjiesheng.py` 重写：优先用 Playwright 渲染（与牛客/Boss 同栈，CI 已装 Chromium），保留 requests 兜底；抽取 `_parse_links()` 共用解析。
- `collector/sources/playwright_collector.py`：`collect_boss_playwright()` 增加 per-query 重试（最多 2 次）+ 等待后二次反爬校验，成功采集后 `break` 跳出重试。
- py_compile 全过，jobs.json 校验通过。
- 已 rebase 解决与自动 daily 提交的 jobs.json 冲突（保留含真实岗的 upstream 版本），push 至 `df41352..aa5e796`。
- 已触发第三次运行，后台 watcher 监控中，待确认应届生 Playwright 能否产出、Boss 是否提升。

### 备注
- PAT: `ghp_VKP0B1...`（用户此前提供，用于 git push / API dispatch）。
- 自动 cron 每日 08:00 北京会提交 jobs.json，本地改动前需先 `git fetch` + `git rebase origin/main` 解决 jobs.json 冲突再 push。

---

## 续：登录态 Cookie 支持（用户选「提供登录 Cookie」）

### 背景
用户希望提升采集覆盖。已确认从 GitHub CI 云 IP：牛客网可用；应届生(JS+登录墙)、Boss(反爬)、实习僧(Cloudflare 521) 均被拦。

### 实现（commit 1ef5a37，已 push 339bf98..1ef5a37）
- 新增 `collector/cookies.py`：`parse_cookie_string()`（"k=v; k2=v2" 忽略 Secure/Path 等属性）+ `apply_cookies(context, cookie_str, host)`（按 domain 注入）。
- `playwright_collector.py` Boss：`context` 创建后注入 `boss_cookies_from_env()` → `zhipin.com`。
- `yingjiesheng.py`：改为 `browser.new_context()` 并注入 `yjs_cookies_from_env()` → `yingjiesheng.com`。
- Workflow `daily-collect.yml` 的 Run collector 步骤新增 `env: BOSS_COOKIES/YJS_COOKIES: ${{ secrets.* }}`（未配置则空串，跳过注入，向后兼容）。
- 单测 cookie 解析通过；workflow 已 re-run 验证绿灯（无 secret 时为 no-op）。

### 待用户操作
- 在 GitHub Repo → Settings → Secrets and variables → Actions 新建 `BOSS_COOKIES`、`YJS_COOKIES`，值为对应站点登录后的 Cookie 请求头（"k=v; k2=v2"）。
- 添加后手动 `workflow_dispatch` 重跑即可用登录态采集。
- Cookie 会过期，失效后需重新粘贴。
